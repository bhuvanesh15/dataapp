import { TabPlaceholder } from "@/components/phase1/TabPlaceholder";

export default function MarketVelocityPage() {
  return (
    <TabPlaceholder
      title="Market Velocity & Trends"
      description="Transaction frequency, price momentum, mini stat cards, and velocity tables (Phase 2)."
    />
  );
}
